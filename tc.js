'use strict';

const emptyArray = [];
const array = [1, 2, 3];

console.log(`Empty: ${size(emptyArray) === emptyArray.length}`)
console.log(`Loaded: ${size(array) === array.length}`)

function size() {
    
}
